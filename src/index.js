import React, { StrictMode } from 'react';
import { render } from 'react-dom';

import './index.css';

render(
  <StrictMode>
    <div>An app</div>
  </StrictMode>,
  document.getElementById('root')
);
